# Elephant Tracks needs NO secrets to run.
# The judged path uses CoinMarketCap's keyless /public-api surface.
#
# Optional — only for the offline holder-series collector, never for the demo:
# CMC_API_KEY=
